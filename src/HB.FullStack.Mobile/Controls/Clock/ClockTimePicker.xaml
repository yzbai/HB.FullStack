<?xml version="1.0" encoding="UTF-8" ?>
<skia:BaseSkiaContentView x:Class="HB.FullStack.Mobile.Controls.Clock.ClockTimePicker"
                          xmlns="http://xamarin.com/schemas/2014/forms"
                          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:base="clr-namespace:HB.FullStack.Mobile.Base;assembly=HB.FullStack.Mobile"
                          xmlns:skia="clr-namespace:HB.FullStack.Mobile.Skia;assembly=HB.FullStack.Mobile"
                          x:Name="root">

    <skia:BaseSkiaContentView.Content>

        <Grid BindingContext="{x:Reference root}">
            <skia:SKFigureCanvasView x:Name="BackgroundCanvasView"
                                     HorizontalOptions="FillAndExpand"
                                     VerticalOptions="FillAndExpand"
                                     IsAnimationMode="True"
                                     Figures="{Binding BackgroundFigures}" />
            <skia:SKFigureCanvasView x:Name="ForegroundCanvasView"
                                     HorizontalOptions="FillAndExpand"
                                     VerticalOptions="FillAndExpand"
                                     IsAnimationMode="False"
                                     Figures="{Binding ForegroundFigures}"
                                     Painted="OnForegroundCanvasViewPainted" />
        </Grid>

    </skia:BaseSkiaContentView.Content>
</skia:BaseSkiaContentView>