<?xml version="1.0" encoding="utf-8" ?>
<base:BaseModalDialog
    x:Class="HB.FullStack.Client.Controls.MultiplePickerDialog"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:base="clr-namespace:HB.FullStack.Client.Base"
    xmlns:controls="clr-namespace:HB.FullStack.Client.Controls"
    x:Name="root"
    IsBackgroudClickedToDismiss="False"
    mc:Ignorable="d">

    <StackLayout BindingContext="{Reference root}">
        <controls:MultipleListPicker x:Name="multiplePicker" ItemsSource="{Binding ItemsSource}" />
        <Button
            Text="确定"
            Clicked="Confirm_Button_Clicked"
            Command="{Binding ConfirmCommand}"
            CommandParameter="{Binding Source={Reference multiplePicker}, Path=SelectedIndexes}" />
    </StackLayout>

</base:BaseModalDialog>